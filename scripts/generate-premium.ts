/**
 * DISABLED (2026-09-06): 이슈칼럼(=premium columns) was removed from the product.
 * Kept as a stub so CI/docs that still call this script fail loudly.
 */
console.error(
  "[premium:generate] disabled — 이슈칼럼 generation was retired. " +
    "Do not restore this pipeline without product approval.",
);
process.exit(1);
