# 검색엔진 등록 · AdSense 전 체크리스트

운영 호스트는 **`https://www.kindexlab.com`** 입니다. apex(`https://kindexlab.com`)는 Vercel에서 www로 308 리다이렉트되므로 Search Console / 서치어드바이저 / sitemap 제출은 **www** 기준으로 진행하세요.

이미 준비된 것:

- `https://www.kindexlab.com/robots.txt`
- `https://www.kindexlab.com/sitemap.xml` (10분 ISR · 품질 게이트 통과 URL만)
- 정책 페이지: `/terms`, `/disclaimer`, `/contact`, `/privacy`
- 소유권 메타: Google·네이버 확인 토큰은 `src/lib/seo-verification.ts`에 반영됨 (env로 덮어쓰기 가능)
- 상세(`/ranking`, `/politics`)는 **체인(Gemini) 분석 + 이름 품질 게이트**를 통과할 때만 `index`
- 랭킹/보드: `ItemList` + `Dataset` + `BreadcrumbList` JSON-LD
- 브리핑: `NewsArticle` JSON-LD (정치 브리핑 포함)
- 상세 ↔ 투데이 브리핑 내부 링크 (`RelatedBriefingLinks`)
- SERP 제목: `실시간 … 지수 및 트렌드 분석` 형태
- `/search`는 robots disallow
- `robots.txt`에 **Yeti**(네이버 로봇) 명시 허용
- `https://www.kindexlab.com/feed.xml` — 투데이 브리핑 본문 중심 RSS (네이버 제출용)

로컬·프로덕션 헬스:

```bash
npm run seo:check
```

## 렌더링 메모 (구글봇 / Yeti)

- 카테고리·보드·브리핑·정치 상세는 RSC/SSR(ISR)로 **완성 HTML**을 제공합니다.
- `/ranking/[slug]`는 시세 실시간성을 위해 `force-dynamic`이지만 서버에서 히어로·요약·브리핑 링크를 먼저 렌더합니다. 차트 캔버스만 클라이언트입니다.

## GSC 현황 해석 (2026-09-18 리포트)

| 지표 | 관찰 | 의미 / 조치 |
|------|------|-------------|
| 클릭 1·노출 ~104·평균순위 ~11 | 콜드스타트 | 브랜드(`킨덱스` 2위) 기회 있음. CTR 개선은 색인 안정화 후 |
| 색인됨 786 / 미색인 1164 | 발견 과다 | 사이트맵·내부링크가 얇은 상세를 너무 많이 광고 |
| 발견됨-색인안됨 ~1003 | 크롤 대기열 | 사이트맵에서 브래킷 레거시·잡 키워드 축소 (코드 반영) |
| 대체 페이지(표준 태그) 82 | `?name=` 중복 | 내부 `entityHref`에서 쿼리 제거 + 미들웨어 308 스트립 |
| NOINDEX 제외 69 | 의도적 게이트 | 체인 분석 미통과 상세 — 정상. 사이트맵에도 넣지 않음 |
| 리디렉션 5 / 404 1 | 소수 | 보드 개명·apex→www. 404는 URL 검사로 확인 |
| 검색어: 총잡이 고양이 공략, 폴킴 키 | 잡음 | 가이드/잡 이름 게이트로 색인·사이트맵 배제 |

배포 후 GSC에서 **sitemap.xml 재제출**, 허브·강한 브리핑·품질 통과 상세만 URL 검사로 색인 요청하세요.

## GSC 현황 해석 (2026-09 초반)

| 지표 | 관찰 | 의미 |
|------|------|------|
| 클릭 0 / 노출 70 / 순위 11.8 | 콜드스타트 | 색인·CTR이 아직 형성되지 않음 |
| 사이트맵 성공 · 발견 1,265 | 제출은 OK | 과거엔 noindex 상세가 대량 포함 → 크롤 낭비 |
| 색인/CWV “처리 중” | 정상 | 속성 초기 며칠~수주 데이터 지연 |
| 검색어: 킨덱스, 인간극장 시청률, 기본소득당 지지율 | 기회 | 브랜드·의도 검색은 상세 색인 개방으로 흡수 |
| 총잡이 고양이 공략, 폴킴 키, 죄송하다 URL | 잡음 | 가이드/잡 URL은 색인·사이트맵에서 배제 |

## 네이버 서치어드바이저 현황 (2026-09-18)

| 지표 | 관찰 | 의미 / 조치 |
|------|------|-------------|
| 수집 완료 4 · 제한 0 | 수집 **초입** | 수개월 0 → 당일 스파이크. 오류(제한) 없음은 긍정적 |
| 다운로드 ~330KB · ~480ms | 정상 | 응답·용량은 허용 범위 |
| 홈 색인 성공 + SEO 경고 `H1 2개` | **구버전 스냅샷** | 프로덕션 홈은 이미 H1 1개(`실시간 랭킹`은 h2). **검증 → 색인 상태 확인 → 수집 요청**으로 재진단 |
| 카테고리 브리핑/아카이브 H1 2개 | 레이아웃 sr-only + 페이지 H1 | `CategoryDeskHeader`에서 board 외 섹션 H1 제거 (코드 반영) |

**지금 할 일 (콘솔):**
1. 홈 URL **수집 요청** 후 색인 상태 재확인 → H1 경고 소멸 확인
2. `/briefing`, 채널 허브 5개, 최근 브리핑 5~10개 **웹 페이지 수집** (하루 한도 내)
3. **사이트맵·RSS**가 제출돼 있는지 재확인 (`/sitemap.xml`, `/feed.xml`)
4. 수집 완료 수가 4 → 수십으로 늘 때까지 2~3일 간격으로 현황 확인

## 네이버 서치어드바이저 현황 (2026-09-16)

| 지표 | 관찰 | 의미 |
|------|------|------|
| 수집 완료 3페이지 · 제한 0 | 수집 **시작** | 3개월 0 → 당일 첫 수집. 오류 없음은 긍정적 |
| 다운로드 ~280KB · 평균 ~500ms | 정상 | 서버 응답은 허용 범위 |
| 노출/클릭 · 사이트 진단 “정보 없음” | 신규 사이트 지연 | 수집→색인→노출까지 추가 시일 필요 |

유입을 늘리려면 **수집량을 먼저** 키워야 합니다. 노출 리포트는 수집·색인 뒤에 채워집니다.

## 1. Google Search Console — 지금 할 일

1. **Sitemaps**에서 `/sitemap.xml` 재제출(배포 후) → “발견된 페이지”가 품질 URL 위주로 재집계되는지 확인 (기존 항목 **삭제 불필요**)
2. **URL 검사 → 색인 생성 요청** (하루 소량, 허브 우선):
   - `/`
   - `/entertainment` `/politics` `/economy` `/culture` `/travel`
   - `/briefing`
   - 강한 브리핑 2~3개, 품질 통과 상세 2~3개 (예: 정당·TV 시청률 키워드)
3. **색인 생성 > 페이지** 리포트가 “처리 중”에서 벗어나면:
   - 색인됨 / 제외됨 사유를 주 1회 확인
   - “크롤됨 - 현재 색인이 생성되지 않음”, “중복”, “소프트 404” 비중을 줄이는 게 목표
4. **실적**에서 노출이 생긴 쿼리·페이지를 주 1회 점검 — CTR 0%면 제목·설명을 키워드 의도에 맞게 조정
5. 제품 스니펫(1회 노출)은 부수적 — KinDex는 지수/랭킹·NewsArticle·Dataset이 본선

## 2. 네이버 서치어드바이저 — 지금 할 일 (유입 핵심)

배포 반영 후 아래 순서로 진행하세요.

**메뉴 위치:** 왼쪽 사이드바 **요청**을 펼치면 네 항목이 보입니다.  
`웹 페이지 수집` · `웹 페이지 검색 제외` · **`RSS 제출`** · **`사이트맵 제출`**  
(「웹 페이지 수집」 화면만 열려 있어도, 그 아래/옆에 RSS·사이트맵 메뉴가 있습니다.)

1. **요청 → 사이트맵 제출** 클릭 → URL: `https://www.kindexlab.com/sitemap.xml`  
   - 이미 있으면 삭제하지 말고 유지. 미제출이면 제출.
2. **요청 → RSS 제출** 클릭 → URL: `https://www.kindexlab.com/feed.xml`  
   - 네이버는 RSS를 “콘텐츠 피드”로 주기 재방문합니다. **브리핑 본문이 들어간 피드**가 중요합니다.
3. **검증 → robots.txt**: 상단 **수집요청**으로 최신본 재수집.  
   - 기대 내용: `User-Agent: Yeti`, `Disallow: /search`, `Sitemap: https://www.kindexlab.com/sitemap.xml`  
   - 예전 스냅샷에 Sitemap/Yeti가 없으면 프로덕션 배포 후 재수집.
4. **요청 → 웹 페이지 수집** (하루 한도 내, 허브 우선) — URL은 **전체 주소**로 입력:
   - `https://www.kindexlab.com/`
   - `https://www.kindexlab.com/briefing`
   - `/entertainment` `/politics` `/economy` `/culture` `/travel` 허브
   - 최근 투데이 브리핑 URL 5~10개  
   - `인간극장 시청률`·`기본소득당 지지율`처럼 의도 검색에 맞는 상세(품질 통과분) 소수  
   - 입력칸에 `briefing`만 넣지 말고 전체 URL을 넣으세요.
5. **검증 → 색인 상태 확인**에서 홈 SEO 경고(`H1` 2개)가 남아 있으면 — 코드상 홈은 이미 H1 1개입니다. **수집 요청**으로 재진단하세요. 카테고리 브리핑/아카이브의 이중 H1도 제거됨 (`npm run` 전 `npx tsx scripts/_check-single-h1.ts`).
6. **수집 현황**을 2~3일 간격으로 확인: 완료 페이지 수가 3 → 수십·수백으로 늘어야 정상.
7. 노출/클릭 리포트가 생기면: 노출 키워드 CTR을 보고 브리핑 제목을 검색 의에 맞게 다듬기.
8. (선택) 서치어드바이저에서 **IndexNow** 키가 있으면 env로 연동해 신규 브리핑 발행 시 알림 — 수집 가속.

### 콘텐츠·유입 운영 팁 (네이버)

- 네이버는 **최신·본문 있는 글(브리핑)** 수집을 선호합니다. 투데이 브리핑을 꾸준히 발행·유지하세요.
- 제목에 `실시간 시청률`, `지지율`, `화제성 지수`처럼 **검색어와 일치하는 가치 문구**를 유지합니다(코드에 반영됨).
- 외부에서 자연 인용(뉴스·커뮤니티·SNS)이 생기면 C-Rank·재방문에 도움이 됩니다. 유료 트래픽보다 **수집·색인 기반**이 먼저입니다.

## 3. AdSense 전에 같이 볼 것

- 이용약관 / 면책 / 문의 / 개인정보가 실제로 열리는지 (`seo:check`에 포함)
- 홈·카테고리 보드에 빈 히트맵이 없는지
- 색인 요청은 품질 확인된 주요 URL 위주

## 코드 쪽 메모

- Google·네이버 확인 토큰: `src/lib/seo-verification.ts`
- 상세 색인 게이트·SERP 제목·Dataset: `src/lib/seo/indexable-entity.ts`
- 상세→브리핑 링크: `src/lib/seo/related-briefings.ts`, `RelatedBriefingLinks`
- 네이버 RSS: `src/app/feed.xml/route.ts`
