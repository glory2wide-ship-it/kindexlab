import type { PostChannel } from "@/lib/posts/types";

/** Hidden until shopping recommendations return after AdSense approval. */
export async function CategoryAffiliate(_props: { channel: PostChannel }): Promise<null> {
  return null;
}
